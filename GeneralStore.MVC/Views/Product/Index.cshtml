@model IEnumerable<GeneralStore.MVC.Models.Product>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table>
    @foreach (var product in Model)
    {
        <tr>
            <td>
                @product.Name
            </td>
        </tr>
    }
</table>

